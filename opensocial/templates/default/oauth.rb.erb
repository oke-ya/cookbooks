Zeroshiki::Application.configure do
  OpensocialWap::Platform.mobage(config) do
    consumer_key    "<%= node[:opensocial][:key]%>"
    consumer_secret "<%= node[:opensocial][:secret]%>"
    app_id          "<%= node[:opensocial][:app_id]%>"
    sandbox         <%= node[:opensocial][:sandbox] ? 'true' : 'false' %>
    session         true
    type            'sp'
  end
end
